# 2025 - 09 - 02

# Alter ( The ALTER TABLE command in SQL is a Data Definition Language  <br>
(DDL) statement used to modify the structure of an existing table in a database. <br> 
It allows for various structural changes without needing to drop and recreate the table <br>

( With help of alter command to changed and modify table and table strucurtes like... 
1. Adding a new column:
2. Drop existing column:
3. rename table and column name 
4. Modifying an existing column ( data type, size , key constraints ) 

-- rename table name and column name 
rename table students to new_Name;

select * from students;
select * from new_Name;

alter table new_Name rename abc;
select * from abc;

-- rename column name
alter table abc rename column email_id to new_email;
select * from abc;

-- add column name 
alter table abc add column new_col varchar(30) not null;
alter table abc add column new_col1 varchar(30) not null default('yes');
alter table abc add column create_at datetime;
select * from abc;

alter table abc drop column  new_col1;
 
describe abc;

alter table abc modify gender varchar(20);
alter table abc modify address text;
